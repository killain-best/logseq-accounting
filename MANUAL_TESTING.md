# Logseq 实机验收清单

每次修改账单写入、DB 查询或界面交互后，先执行 `npm run check`，再在 Logseq 插件页重载「日志记账」。

## 1. 启动与兼容性

- [ ] DB graph 中插件正常启动，工具栏显示 Logseq 原生风格的报表图标。
- [ ] 首次启动及以后重载均不弹出教学窗口。
- [ ] 插件设置页显示可编辑的使用引导，包含 `/expense`、`/income`、`/assets`、`/report`。
- [ ] 输入 `/edit` 不再出现插件命令；账单悬停“编辑”和右键编辑仍可用。
- [ ] 属性中存在金额、类型、分类，且 key 仍为 `txn_*`；旧账户属性不被删除。
- [ ] 在非 DB graph 中加载时显示不支持提示，不注册记账功能。

## 2. 账单录入

- [ ] 空日志块输入 `/expense`，账单就地写入当前块。
- [ ] 非空日志块输入 `/expense`，账单出现在原块之后且与原块同级。
- [ ] `/income` 生成收入块，标题使用 💰，类型为「收入」。
- [ ] 金额遵循设置中的小数位数；空值、0 和负数无法提交。
- [ ] 分类下拉可用，也能输入新的中文值。
- [ ] 新分类作为 node 属性写入，可点击进入对应页面。
- [ ] 账单块带 `#账单` 标签，日志中显示卡片样式。

## 3. 报表与刷新

- [ ] 工具栏报表图标和 `/report` 都能打开仪表盘。
- [ ] 月份前后导航正确，概览、明细、资产三个页面尺寸一致。
- [ ] 明细搜索、类型/分类筛选以及日期/金额排序组合后结果仍正确。
- [ ] 资产页日历位于左侧约 1/3，右侧按“净资产 = 总资产 − 总负债”展示。
- [ ] 设置语言为 English 并重载后，界面、工具栏提示与右键菜单显示英文。
- [ ] 设置 0–4 位小数后，表单、流水、图表和资产盘点金额一致。
- [ ] 自定义支出、收入和盘点颜色后，报表与财务日历使用新颜色。
- [ ] 当月支出、收入、结余与日志账单一致。
- [ ] 分类环形图和近 6 个月柱状图显示正常。
- [ ] 连续修改多条账单属性时，报表在稍后刷新，不出现旧数据覆盖新数据。
- [ ] 明细按日倒序分组，点击明细可跳回原日志块。
- [ ] 非日志页上的带金额块不纳入统计。

## 4. 资产盘点

- [ ] 日志中输入 `/assets` 打开双栏大盘点界面。
- [ ] 首次使用包含约定的资产、负债父类和子类。
- [ ] 父类右侧＋可新增子类，父类和子类菜单可重命名、删除。
- [ ] 保存后今日日志只新增一条 `#资产盘点` 快照块。
- [ ] 再次打开时输入值沿用上次盘点，并显示上次盘点时间和金额。
- [ ] 删除子类并保存后，历史快照仍可查看；重新添加同父类同名子类时接续最近金额。
- [ ] 报表「资产」页显示总资产、总负债、净资产和最新结构。
- [ ] 修改币种和分类设置后，重新打开界面会使用新值。

## 5. 界面

- [ ] Esc、关闭按钮和点击遮罩都能关闭界面。
- [ ] Logseq 亮色/暗色主题切换后界面正常跟随。
- [ ] 关闭正在刷新的报表时，控制台没有 React 卸载后更新警告。

## 失败行为约定

如果 Logseq 在块已创建后拒绝某个属性写入，插件不会自动删除块。该块会加上「⚠️ 未完整保存」前缀，插件同时显示失败阶段并关闭表单。请手动检查和补全已保留的块，不要从原表单重复提交，以免生成重复账单。
